<div class="row align-items-center text-primary m-2">
  <h3>{{playlist.name}}</h3>
  <a href="/playlist-details/{{{playlistID}}}/add-song" class="btn btn-outline-primary m-3">+ ADD</a>
  {{!-- <a href="/playlist-details/{{{playlistID}}}/push" class="btn btn-outline-primary">PUSH TO SPOTIFY</a> --}}
</div>

<table class="table table-dark">
  <thead>
    <tr>
      <th scope="col">NAME</th>
      <th scope="col">ARTIST</th>
      <th scope="col">VOTES</th>
      <th scope="col"></th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>

    {{#each track}}
    <tr>
      <th scope="row">{{{this.name}}}</th>
      <td>{{{this.artist}}}</td>
      <td>{{{this._userWhoVoted.length}}}</td>
      {{#if ../user}}
      <td><a href="/vote/{{this.spotifyTrackId}}/{{../playlistID}}" class="btn btn-outline-secondary">Vote</a></td>
      {{!-- <a><a href="#" class="btn btn-outline-primary">Delete</a></a> --}}
      {{/if}}
    </tr>
    {{/each}}
  </tbody>
</table>